import {useEffect} from "react";
import {useParams} from "react-router-dom";

const BlogDetails = () => {
    const {id} = useParams()
    useEffect(()=>{

        // ()()
    })
    return (
        <div>
            
        </div>
    );
};

export default BlogDetails;